name: chitchat
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: "icon"
  ios: true
  image_path: "img/logo.jpg"
  min_sdk_android: 21

flutter:
  uses-material-design: true
  assets:
  - img/logo.jpg
