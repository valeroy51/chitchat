import 'package:flutter/material.dart';

class Cobascreen extends StatelessWidget {
  const Cobascreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("CobaScreen"),
      ),
    );
  }
}
